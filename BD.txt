-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.arrows (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  series_id uuid NOT NULL,
  arrow_number integer NOT NULL,
  score integer NOT NULL CHECK (score >= 0 AND score <= 10),
  is_x boolean DEFAULT false,
  position_x numeric,
  position_y numeric,
  direction_error USER-DEFINED DEFAULT 'centro'::direction_error,
  notes text,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT arrows_pkey PRIMARY KEY (id),
  CONSTRAINT arrows_series_id_fkey FOREIGN KEY (series_id) REFERENCES public.series(id)
);
CREATE TABLE public.athlete_goals (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  athlete_id uuid NOT NULL,
  coach_id uuid,
  title character varying NOT NULL,
  description text,
  target_date date,
  status character varying DEFAULT 'activo'::character varying,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT athlete_goals_pkey PRIMARY KEY (id),
  CONSTRAINT athlete_goals_athlete_id_fkey FOREIGN KEY (athlete_id) REFERENCES public.athlete_profiles(id),
  CONSTRAINT athlete_goals_coach_id_fkey FOREIGN KEY (coach_id) REFERENCES public.users(id)
);
CREATE TABLE public.athlete_parents (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  athlete_id uuid NOT NULL,
  parent_user_id uuid NOT NULL,
  relationship character varying,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT athlete_parents_pkey PRIMARY KEY (id),
  CONSTRAINT athlete_parents_athlete_id_fkey FOREIGN KEY (athlete_id) REFERENCES public.athlete_profiles(id),
  CONSTRAINT athlete_parents_parent_user_id_fkey FOREIGN KEY (parent_user_id) REFERENCES public.users(id)
);
CREATE TABLE public.athlete_profiles (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL UNIQUE,
  coach_id uuid,
  birthdate date,
  gender character varying,
  category character varying,
  bow_type USER-DEFINED NOT NULL DEFAULT 'recurvo'::bow_type,
  dominant_hand USER-DEFINED,
  notes text,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  photo_url text,
  curp text,
  club_id uuid,
  association_id text,
  federation_id text,
  CONSTRAINT athlete_profiles_pkey PRIMARY KEY (id),
  CONSTRAINT athlete_profiles_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT athlete_profiles_coach_id_fkey FOREIGN KEY (coach_id) REFERENCES public.users(id),
  CONSTRAINT athlete_profiles_club_id_fkey FOREIGN KEY (club_id) REFERENCES public.clubs(id)
);
CREATE TABLE public.athlete_skill_evaluations (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  athlete_id uuid NOT NULL,
  coach_id uuid,
  skill_id uuid NOT NULL,
  level integer NOT NULL CHECK (level >= 1 AND level <= 5),
  comments text,
  evaluated_at date NOT NULL DEFAULT CURRENT_DATE,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT athlete_skill_evaluations_pkey PRIMARY KEY (id),
  CONSTRAINT athlete_skill_evaluations_athlete_id_fkey FOREIGN KEY (athlete_id) REFERENCES public.athlete_profiles(id),
  CONSTRAINT athlete_skill_evaluations_coach_id_fkey FOREIGN KEY (coach_id) REFERENCES public.users(id),
  CONSTRAINT athlete_skill_evaluations_skill_id_fkey FOREIGN KEY (skill_id) REFERENCES public.skills(id)
);
CREATE TABLE public.clubs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name character varying NOT NULL,
  city character varying,
  state character varying,
  country character varying DEFAULT 'México'::character varying,
  logo_url text,
  is_active boolean DEFAULT true,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT clubs_pkey PRIMARY KEY (id)
);
CREATE TABLE public.conade_marks (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  year integer NOT NULL,
  category character varying NOT NULL,
  gender character varying NOT NULL,
  bow_type character varying NOT NULL,
  distance_meters integer NOT NULL,
  target_size_cm integer,
  arrows_count integer DEFAULT 72,
  minimum_score integer NOT NULL,
  notes text,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT conade_marks_pkey PRIMARY KEY (id)
);
CREATE TABLE public.equipment_profiles (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  athlete_id uuid NOT NULL,
  name character varying DEFAULT 'Equipo principal'::character varying,
  bow_brand character varying,
  riser_model character varying,
  limbs_model character varying,
  bow_length_inches integer,
  draw_weight_lbs numeric,
  arrow_brand character varying,
  arrow_model character varying,
  spine character varying,
  arrow_length_inches numeric,
  point_weight_grains numeric,
  brace_height_cm numeric,
  tiller_top_cm numeric,
  tiller_bottom_cm numeric,
  nocking_point_mm numeric,
  button_settings text,
  sight_settings text,
  stabilizer_settings text,
  is_active boolean DEFAULT true,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  bow_type text,
  bow_model text,
  riser_size text,
  limb_brand text,
  vane_type text,
  nock_type text,
  sight_brand text,
  sight_model text,
  rest_brand text,
  rest_model text,
  plunger_brand text,
  plunger_model text,
  stabilizer_brand text,
  stabilizer_setup text,
  string_material text,
  string_strands integer,
  notes text,
  riser_brand character varying,
  riser_length_inches integer,
  limb_length character varying,
  draw_length_inches numeric,
  rest_type text,
  sight_pin_type text,
  clicker_brand text,
  clicker_model text,
  tab_brand text,
  tab_model text,
  CONSTRAINT equipment_profiles_pkey PRIMARY KEY (id),
  CONSTRAINT equipment_profiles_athlete_id_fkey FOREIGN KEY (athlete_id) REFERENCES public.athlete_profiles(id)
);
CREATE TABLE public.equipment_tuning_logs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  equipment_profile_id uuid NOT NULL,
  athlete_id uuid NOT NULL,
  coach_id uuid,
  tuning_date date NOT NULL DEFAULT CURRENT_DATE,
  brace_height_cm numeric,
  tiller_top_cm numeric,
  tiller_bottom_cm numeric,
  nocking_point_mm numeric,
  button_settings text,
  sight_settings text,
  change_description text,
  observed_result text,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT equipment_tuning_logs_pkey PRIMARY KEY (id),
  CONSTRAINT equipment_tuning_logs_equipment_profile_id_fkey FOREIGN KEY (equipment_profile_id) REFERENCES public.equipment_profiles(id),
  CONSTRAINT equipment_tuning_logs_athlete_id_fkey FOREIGN KEY (athlete_id) REFERENCES public.athlete_profiles(id),
  CONSTRAINT equipment_tuning_logs_coach_id_fkey FOREIGN KEY (coach_id) REFERENCES public.users(id)
);
CREATE TABLE public.series (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  training_round_id uuid NOT NULL,
  series_number integer NOT NULL,
  total_score integer DEFAULT 0,
  average_score numeric DEFAULT 0,
  group_size_cm numeric,
  notes text,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT series_pkey PRIMARY KEY (id),
  CONSTRAINT series_training_round_id_fkey FOREIGN KEY (training_round_id) REFERENCES public.training_rounds(id)
);
CREATE TABLE public.skills (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name character varying NOT NULL,
  category character varying,
  description text,
  is_active boolean DEFAULT true,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT skills_pkey PRIMARY KEY (id)
);
CREATE TABLE public.training_rounds (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  training_session_id uuid NOT NULL,
  round_number integer NOT NULL,
  distance_meters integer NOT NULL,
  target_size_cm integer,
  total_series integer,
  arrows_per_series integer DEFAULT 6,
  notes text,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT training_rounds_pkey PRIMARY KEY (id),
  CONSTRAINT training_rounds_training_session_id_fkey FOREIGN KEY (training_session_id) REFERENCES public.training_sessions(id)
);
CREATE TABLE public.training_sessions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  club_id uuid,
  athlete_id uuid NOT NULL,
  coach_id uuid,
  equipment_profile_id uuid,
  training_date date NOT NULL,
  start_time time without time zone,
  end_time time without time zone,
  location character varying,
  session_type character varying,
  weather USER-DEFINED DEFAULT 'otro'::weather_type,
  wind_speed_kmh numeric,
  temperature_c numeric,
  humidity_percent numeric,
  objective text,
  coach_notes text,
  athlete_notes text,
  total_score integer DEFAULT 0,
  total_arrows integer DEFAULT 0,
  average_score numeric DEFAULT 0,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  status text DEFAULT 'draft'::text,
  brace_height_cm numeric,
  CONSTRAINT training_sessions_pkey PRIMARY KEY (id),
  CONSTRAINT training_sessions_club_id_fkey FOREIGN KEY (club_id) REFERENCES public.clubs(id),
  CONSTRAINT training_sessions_athlete_id_fkey FOREIGN KEY (athlete_id) REFERENCES public.athlete_profiles(id),
  CONSTRAINT training_sessions_coach_id_fkey FOREIGN KEY (coach_id) REFERENCES public.users(id),
  CONSTRAINT training_sessions_equipment_profile_id_fkey FOREIGN KEY (equipment_profile_id) REFERENCES public.equipment_profiles(id)
);
CREATE TABLE public.users (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  club_id uuid,
  name character varying NOT NULL,
  email character varying NOT NULL UNIQUE,
  password_hash text,
  role USER-DEFINED NOT NULL,
  phone character varying,
  profile_photo_url text,
  is_active boolean DEFAULT true,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT users_pkey PRIMARY KEY (id),
  CONSTRAINT users_club_id_fkey FOREIGN KEY (club_id) REFERENCES public.clubs(id)
);